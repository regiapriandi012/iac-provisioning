# Environment-Agnostic Configuration
# This file contains configuration values that can be customized per environment

# Git Repository Configuration
# DEFAULT_GIT_REPOSITORY=https://gitlab.labngoprek.my.id/root/iac-provision
# Note: This is now configured via Jenkins parameters
GIT_BRANCH=main
GIT_CREDENTIALS_ID=gitlab-credential

# Proxmox Configuration
DEFAULT_PROXMOX_NODE=thinkcentre
DEFAULT_VM_TEMPLATE=t-debian12-ultra-k8s
DEFAULT_CENTOS_TEMPLATE=t-centos9-ultra-k8s

# VM Configuration Defaults
DEFAULT_CORES=2
DEFAULT_MEMORY=2048
DEFAULT_DISK_SIZE=10G

# Cluster Configuration
DEFAULT_MASTER_COUNT=3
DEFAULT_WORKER_COUNT=2

# Kubernetes Configuration
DEFAULT_KUBERNETES_VERSION=1.32.7
DEFAULT_POD_NETWORK_CIDR=10.244.0.0/16
DEFAULT_SERVICE_CIDR=10.96.0.0/12
DEFAULT_CONTAINER_RUNTIME=containerd

# CNI Configuration
DEFAULT_CNI_TYPE=cilium
DEFAULT_CNI_VERSION=1.16.0

# Network Configuration
DEFAULT_IP_RANGE_START=10.200.0.0/24

# User Configuration (adjust based on target environment)
DEFAULT_ANSIBLE_USER=root
DEFAULT_SSH_USER=root

# Performance Configuration
DEFAULT_FORKS=100
DEFAULT_PARALLELISM=50

# Jenkins Pipeline Parameters (used by Jenkinsfile)
RUN_ANSIBLE=true
USE_CACHE=true
GIT_REPOSITORY_URL=https://gitlab.labngoprek.my.id/root/iac-provision
PROXMOX_CREDENTIALS_PREFIX=proxmox
SLACK_WEBHOOK_CREDENTIAL_ID=slack-webhook-url

# ===== Deployment Mode Configuration =====
# Choose deployment optimization strategy:
# - ULTRA_OPTIMIZED_DEPLOYMENT=true: Use ULTRA pre-baked templates (fastest: ~25-30s) - 5-7x faster
# - TEMPLATE_DEPLOYMENT=true: Use pre-baked VM templates (fast: ~50s) - 3.5x faster
# - PARALLEL_DEPLOYMENT=true: Use parallel playbooks (~120s)
# - All false: Standard sequential deployment (~180s baseline)

# ULTRA-optimized deployment with maximum pre-baking (ULTRA-FAST)
ULTRA_OPTIMIZED_DEPLOYMENT=true

# Template-optimized deployment with pre-baked VMs (fallback)
TEMPLATE_DEPLOYMENT=true

# Parallel deployment (fallback if templates not available)
PARALLEL_DEPLOYMENT=false
